<Window x:Class="UnoEdit.Skia.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:UnoEdit.Skia.Desktop.Controls"
        xmlns:avalonedit="using:ICSharpCode.AvalonEdit"
        xmlns:pg="using:UnoPropertyGrid"
        xmlns:toolkit="using:CommunityToolkit.WinUI.Controls">
  <Grid Padding="20"
        RowSpacing="16"
        Loaded="OnRootGridLoaded">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Spacing="4">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Spacing="2">
          <TextBlock FontSize="28"
                     FontWeight="SemiBold"
                     Text="UnoEdit Desktop Bootstrap" />
          <TextBlock Opacity="0.72"
                     Text="This host is using the portable UnoEdit document core from the ported AvalonEdit model." />
        </StackPanel>
        <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="8"
                    VerticalAlignment="Center">
          <ComboBox x:Name="HighlighterComboBox"
                    SelectionChanged="OnHighlighterChanged">
            <ComboBoxItem Content="TextMate" />
            <ComboBoxItem Content="XSHD" />
            <ComboBoxItem Content="None" />
          </ComboBox>
          <Button x:Name="FindButton"
                  Content="Find"
                  Click="OnFindClick" />
          <Button x:Name="CompleteButton"
                  Content="Complete"
                  Click="OnCompleteClick" />
          <ToggleButton x:Name="ThemeToggle"
                        Content="&#x2600; Light"
                        Click="OnThemeToggleClick" />
        </StackPanel>
      </Grid>
    </StackPanel>

    <Border Grid.Row="1"
            Padding="12"
            CornerRadius="8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
      <TextBlock x:Name="StatsTextBlock"
                 AutomationProperties.AutomationId="StatsTextBlock"
                 FontFamily="Consolas"
                 TextWrapping="WrapWholeWords" />
    </Border>

    <Border Grid.Row="2"
            Padding="10"
            CornerRadius="8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
      <StackPanel Spacing="8">
        <StackPanel Orientation="Horizontal"
                    Spacing="8">
          <Button Content="Load 10k"
                  Click="OnLoad10kClick" />
          <Button Content="Load 100k"
                  Click="OnLoad100kClick" />
          <Button Content="Load 250k"
                  Click="OnLoad250kClick" />
          <Button Content="Top"
                  Click="OnScrollHomeClick" />
          <Button Content="End"
                  Click="OnScrollEndClick" />
        </StackPanel>
        <StackPanel Orientation="Horizontal"
                    Spacing="12">
          <CheckBox x:Name="WordWrapCheckBox"
                    Content="Word wrap"
                    Checked="OnPerfOptionChanged"
                    Unchecked="OnPerfOptionChanged" />
          <CheckBox x:Name="LineNumbersCheckBox"
                    Content="Line numbers"
                    IsChecked="True"
                    Checked="OnPerfOptionChanged"
                    Unchecked="OnPerfOptionChanged" />
          <CheckBox x:Name="BreakpointMarginCheckBox"
                    Content="Breakpoint margin"
                    IsChecked="True"
                    Checked="OnPerfOptionChanged"
                    Unchecked="OnPerfOptionChanged" />
          <CheckBox x:Name="FoldMarginCheckBox"
                    Content="Fold margin"
                    IsChecked="True"
                    Checked="OnPerfOptionChanged"
                    Unchecked="OnPerfOptionChanged" />
          <TextBlock x:Name="PerfStatusTextBlock"
                     VerticalAlignment="Center"
                     FontFamily="Consolas"
                     Opacity="0.8" />
        </StackPanel>
      </StackPanel>
    </Border>

    <!-- Editor + Property panel -->
    <Grid Grid.Row="3">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" MinWidth="200" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="320" MinWidth="200" />
      </Grid.ColumnDefinitions>

      <avalonedit:TextEditor x:Name="Editor"
                           ShowLineNumbers="True"
                           AutomationProperties.AutomationId="MainEditor" />

      <toolkit:GridSplitter Grid.Column="1"
                            Width="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Stretch"
                            ResizeDirection="Columns" />

      <Grid Grid.Column="2" RowSpacing="8">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ComboBox x:Name="PropertyObjectComboBox"
                  HorizontalAlignment="Stretch"
                  SelectionChanged="OnPropertyObjectChanged">
          <ComboBoxItem Content="TextEditor" />
          <ComboBoxItem Content="TextArea" />
          <ComboBoxItem Content="Options" />
        </ComboBox>
        <pg:PropertyGridControl x:Name="PropertyGrid"
                                Grid.Row="1" />
      </Grid>
    </Grid>
  </Grid>
</Window>
