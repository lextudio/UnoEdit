<UserControl x:Class="UnoEdit.Skia.Desktop.Controls.TextEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:UnoEdit.Skia.Desktop.Controls">
  <Border x:Name="EditorBorder"
          Padding="1"
          BorderBrush="#334155"
          BorderThickness="1"
          CornerRadius="10"
          Background="#111827">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Grid x:Name="TitleBarGrid"
            Padding="12,10"
            Background="#0F172A">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="TitleTextBlock"
                   Text="TextEditor"
                   Foreground="#E5E7EB"
                   FontSize="13"
                   FontWeight="SemiBold" />
        <TextBlock x:Name="SummaryTextBlock"
                   Grid.Column="1"
                   FontFamily="Consolas"
                   Foreground="#94A3B8"
                   FontSize="12" />
      </Grid>

      <controls:SearchPanel x:Name="PART_SearchPanel"
                            Grid.Row="1"
                            Visibility="Collapsed" />

      <controls:TextArea x:Name="PART_TextArea"
                         AutomationProperties.AutomationId="TextArea"
                         Grid.Row="2" />
    </Grid>
  </Border>
</UserControl>
